package com.searchblog.api.domain;

import static org.junit.jupiter.api.Assertions.*;

class SearchBlogTest {
    /**
     * 현재 프로젝트의 도메인 레이어에서는 단위테스트가 필요할 만한 비즈니스 로직이 존재하지 않는다.
     * 이에 통합테스트 관점에서 도메인과 엔티티의 적합한 매핑이 이뤄지면서 처리되는지 확인하였습니다.
     */

}